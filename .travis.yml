language: cpp

sudo: required
dist: trusty

matrix:
    include:
        - compiler: gcc

before_install:
  - sudo apt-get update -qq
  - sudo apt-get install -qq qt5-default qttools5-dev-tools graphviz-dev

script:
  - qmake
  - make
